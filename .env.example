# Local development database (plain `next dev` / Prisma CLI).
# In production on Cloudflare, the connection is provided by the HYPERDRIVE
# binding (see wrangler.jsonc) — this variable is the local fallback.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/limpieza_jci?schema=public"
