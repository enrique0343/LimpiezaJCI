# Local SQLite file for Prisma CLI tooling (generate / migrate diff).
# In production the database is Cloudflare D1, reached via the `DB` binding
# (see wrangler.jsonc) — this variable is only for local Prisma commands.
DATABASE_URL="file:./dev.db"
